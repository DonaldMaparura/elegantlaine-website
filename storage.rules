rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Product images — admin only (authenticated)
    match /products/{imageId} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
